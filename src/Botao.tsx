import { isAbsolute } from "path"

export const Botao = () => {
    return (
        <div style={{
            display: "grid",
            justifyContent: "center",
            alignContent: "center"
        }}>
            <button className={"btn btn-dark mt-5"}>Testando Bootstrap</button>
            </div>
    )
}